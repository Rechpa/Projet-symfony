{% extends 'base.html.twig' %}


{% block body %}
<div class="wrapped" style = " margin: 10px 50px 20px;">


{{form_start(formProduct)}}


{{form_widget(formProduct)}}


<button type="submit" class="btn btn-success">
Submit
</button>

{{form_end(formProduct)}}

</div>

{% endblock %}
